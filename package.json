{
  "name": "torrent-client",
  "version": "1.0.0",
  "description": "torrent client from scratch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npx tsc",
    "start": "node ./dist/index.js",
    "build:start": "npm run clean && npm run build && npm run start"
  },
  "keywords": [
    "bittorrent",
    "torrent",
    "protocol"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.13.10"
  }
}
